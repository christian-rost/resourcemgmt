[phases.setup]
nixPkgs = ["python312", "gcc", "cairo", "pkg-config"]

[phases.install]
cmds = ["python3 -m venv /opt/venv", "/opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port 8003"
